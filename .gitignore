node_modules
.next
.env.local
.env*.local
.vercel
supabase/seed/*.json
# ...except the calendar, which is written by hand rather than generated. Both
# scripts/seed.mjs and scripts/ingest_overviews.py read it, so a checkout without it
# cannot seed or import at all. curriculum.json and readiness_report.json stay ignored:
# those are output of `npm run ingest` and one of them is 790 KB.
!supabase/seed/calendar.json
tsconfig.tsbuildinfo
.gatecheck

.vercel
.env*.local

# dev builds under a second dist dir, and the scratch render checks
.next-dev*
.render-check/
.print_check.mjs
__pycache__/
.next-build
